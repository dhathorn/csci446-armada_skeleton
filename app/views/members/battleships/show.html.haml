= render :partial => 'battle_ship_bar'
%p 
  = "Created by #{@battleship.user.full_name} on #{@battleship.created_at}"
%p= image_tag(@battleship.image :size => :large)
="Name: #{@battleship.name}"
%br
=@battleship.description
